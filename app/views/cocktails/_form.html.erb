<%= simple_form_for @cocktail do |f| %>
  <%= f.input :name %>
  <%= f.association :ingredients, collection: @ingredients, prompt: "Choose your ingredients" %>
  <%= f.association :doses %>
  <%= f.submit class: 'btn btn-primary' %>
<% end %>
